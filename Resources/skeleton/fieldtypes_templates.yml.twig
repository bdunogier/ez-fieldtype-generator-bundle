system:
    default:
        fielddefinition_edit_templates:
            - { template: "{{ target_bundle }}::fieldtypes_templates.html.twig", priority: 0 }
        field_templates:
            - { template: "{{ target_bundle }}::fieldtypes_templates.html.twig"}
